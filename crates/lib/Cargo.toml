[package]
name = "cellang"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
serde = { version = "^1" }
miette = { version="^7"}

[dev-dependencies]
miette = { version="^7", features = ["fancy"] }
