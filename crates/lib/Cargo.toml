[package]
name = "cellang"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
serde = { version = "^1", features = ["derive"] }
miette = { version="^7"}
serde_json = "^1"
regex = "^1"

[dev-dependencies]
miette = { version="^7", features = ["fancy"] }
